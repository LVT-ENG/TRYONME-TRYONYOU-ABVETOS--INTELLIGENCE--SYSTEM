# AGENTS.md — Definición de agentes / contexto del proyecto

## 🎯 Objetivo general  
Este archivo define las reglas, estructura, estilo y convenciones que un agente (IA / desarrollador) debe seguir para trabajar en este proyecto. Sirve como “contrato de estilo + guía de contexto + límites”.  

---

## 🔧 Stack & Tech (válido para todas las plantillas)  
- Framework / Librería frontend: **React** (o **Next.js** si decides compilar / render server-side)  
- Estilos / CSS: **Tailwind CSS** (u otro sistema de utilidades CSS similar)  
- Carpeta para assets públicos: `/public`, con subcarpetas para imágenes, vídeos, logo, etc.  
- Build: `npm run build` (o el comando que uses)  
- Deploy sugerido: automático vía Vercel (detecta automáticamente Next.js / React), con configuración en `vercel.json` (opcional).  

---

## 🧩 Estructura general de carpetas (common para todas)  

