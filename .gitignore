# ============================================
# TRYONYOU–ABVETOS–ULTRA–PLUS–ULTIMATUM
# Auto-protected by Agente 70 (Manus config)
# ============================================

# Build & Deploy artifacts
dist/
node_modules/

# Compressed archives (keep deployment packages out)
*.tar
*.gz

# Local scripts (keep local versions, not deploy scripts)
deploy_express_total_for_manus.sh

# Temporary backups
*_BACKUP/
Desktop/
tmp/

# Logs (keep logs out of repo)
logs/
*.log

# Lock & cache
package-lock.json
.env.local
.env.production
.env.development

# Large media files (exclude from version control)
# Note: Keep specific documentation images and SVGs
*.mov
*.mp4
public/**/*.png
public/**/*.jpg
public/**/*.jpeg
!docs/**/*.png
!docs/**/*.jpg
!docs/**/*.jpeg
!docs/**/*.svg
